# haido — DB local; tri thức đi theo docs/memory-pack/ (commit được)
.haido/

# Node
node_modules/
dist/
coverage/
*.tsbuildinfo

# Dự án nhà tạo ra khi chạy thử Atelier (không thuộc repo này)
sandbox/
/.atelier/
/atelier.project.json

# Editor / OS
.DS_Store
Thumbs.db
